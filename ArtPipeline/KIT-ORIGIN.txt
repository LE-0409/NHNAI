# Where this ArtPipeline kit came from (written by install_kit.ps1).
# If a copy looks out of date, compare 'commit' and reinstall with -Force from a newer source.
#
# NOTE: the 'source' line originally held an absolute path on the author's machine.
#       It was cleared because this repository is public — the commit hash below is
#       what staleness checks actually need. install_kit.ps1 writes the path again on
#       every reinstall, so re-check this file before committing after a kit update.
source : (cleared — local path)
commit : 8744eda
date   : 2026-07-28 04:50:47
