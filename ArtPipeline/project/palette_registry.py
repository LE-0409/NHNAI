"""팔레트 레지스트리 — 이 프로젝트의 색과 공유 머티리얼 슬롯.

NHNAI 는 **완전 흑백**이다. 무채색 명도 10단계만 쓴다.

왜 세피아·쿨톤을 섞지 않는가: 앤틱한 색조는 URP 포스트 프로세싱(Color Adjustments ·
Split Toning · Tonemapping)에서 **한 곳으로** 낸다. 에셋에 톤을 구워버리면 되돌릴 수
없고, 색조를 바꿔보려면 전 에셋을 다시 뽑아야 한다. 여기는 순수 명도만 담는다.

⚠️ COLORS 의 **선언 순서 = 셀 인덱스**다. 순서를 바꾸거나 중간에 끼워 넣으면 이미
익스포트된 전 에셋의 UV 가 어긋난다. 색 추가는 반드시 맨 뒤에만. 안 쓰게 된 색도
지우지 말 것(뒤 인덱스가 전부 한 칸씩 밀린다) — 주석으로 '미사용' 표시만 남긴다.
"""

GRID = 8          # 8×8 셀
CELL_PX = 8       # 셀당 픽셀 → 64×64 이미지

MAT_NAME = "M_Palette"
UNUSED_CELL_RGB = (0.85, 0.30, 0.75)  # 미할당 셀은 눈에 띄는 마젠타 — UV 실수 즉시 발견용

# 이름 → sRGB(0~1). 셀 인덱스는 선언 순서 (좌상단부터 가로 방향).
#
# 명도 사다리. 어두운 쪽이 촘촘한 이유는 게임 화면의 대부분이 어둠이고,
# 전등에서 먼 표면끼리의 미세한 차이가 실루엣을 만들기 때문이다.
COLORS = {
    "void":     (0.02, 0.02, 0.02),   # 어둠에 잠긴 면. 사실상 검정
    "ink":      (0.07, 0.07, 0.07),   # 벽 · 천장 기본
    "charcoal": (0.13, 0.13, 0.13),   # 바닥 · 그늘진 금속
    "iron":     (0.22, 0.22, 0.22),   # 슬롯머신 캐비닛 본체
    "ash":      (0.33, 0.33, 0.33),   # 몰딩 · 프레임
    "concrete": (0.46, 0.46, 0.46),   # 중간 톤 면
    "bone":     (0.60, 0.60, 0.60),   # 릴 드럼 · 밝은 금속
    "paper":    (0.74, 0.74, 0.74),   # 마퀴 패널 · 라벨
    "chalk":    (0.87, 0.87, 0.87),   # 빛을 정면으로 받는 면
    "glare":    (1.00, 1.00, 1.00),   # 전구 · 발광 요소
}

# 팔레트 외 공유 머티리얼 슬롯. Blender 쪽 값은 **미리보기 근사치일 뿐**이고,
# 실제 룩은 Unity 머티리얼이 결정한다. 슬롯 이름으로 식별해 리맵한다.
SLOTS = {
    # 전구 · 발광 요소. Unity 에서 Emission 을 켠 Unlit 계열로 리맵한다.
    "M_Emissive": {
        "alias": "use_emissive_material",
        "default_cell": "glare",
        "roughness": 1.0,
    },
    # 슬롯머신 릴 창 유리. 반사보다 '뿌옇게 가린다'는 느낌이 중요하다.
    "M_Glass": {
        "alias": "use_glass_material",
        "default_cell": "bone",
        "roughness": 0.15,
        "alpha": 0.35,
        "blended": True,
    },
    # 전등 아래 빛 원뿔. URP 에는 볼류메트릭 라이팅이 없어서 메시로 대신한다.
    # Unity 에서 가산(additive) · 깊이 기록 없음 · 양면으로 리맵한다.
    "M_LightCone": {
        "alias": "use_lightcone_material",
        "default_cell": "chalk",
        "roughness": 1.0,
        "alpha": 0.12,
        "blended": True,
    },
}
